[tool.poetry]
name = "trackfit"
version = "0.1.0"
description = "An application for gym performance calculator"
authors = [
    {name = "CarlosOchoa8", email = "ochoa.carlos8@outlook.com"}
]
license = {text = "Apache-2.0"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.115.12,<0.116.0)",
    "uvicorn (>=0.34.2,<0.35.0)",
    "pytest (>=8.3.5,<9.0.0)"
]

# Cambiar la configuración de paquetes a solo src
packages = [{ include = "trackfit", from = "src" }]
[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
